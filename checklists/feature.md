# Checklist: feature
- [ ] Matches the task as written (re-read it now); assumptions marked `ASSUMPTION:`
- [ ] Code placed per layering placement test; dependency rules clean
- [ ] Tests: new behavior + one failure path; all green
- [ ] Errors handled per standard; success + failure log lines present; metric if request-path
- [ ] No secrets/PII in code, logs, or tests
- [ ] Context doc updated if domain knowledge changed
- [ ] Diff contains nothing unrelated to the task
