{
  "name": "harness-template",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build -w @harness/core",
    "test": "npm run test -w @harness/core"
  }
}
