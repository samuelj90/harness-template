# harness-template

A generic **outer harness** for coding agents — guides, sensors, and hooks that steer the agent loop toward consistent, reviewable output. Copy a topology template into your project and adapt.

Work in progress. See `docs/` once the first template lands.
