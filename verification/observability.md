# Verify: observability
✓ Success path logs a business-relevant event with correlation id
✓ Failure paths log error with cause, exactly once per failure
✓ Request-path changes carry a count + latency metric
✓ A responder at 3am could locate this failure from logs alone (say how)
