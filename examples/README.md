# Examples

Agents learn more from one concrete annotated example than from pages of rules. `good/` is imitation material; `bad/` is recognition material — each bad example names the smell, shows a minimal instance, and links the rule it violates.

Grow this directory from real code: when review catches a great pattern or a recurring mistake, distill it here (sanitized). Every example ≤ 60 lines + annotation.
